import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00497F',
      // light: deepPurple[300],
      // main: deepPurple[500],
      // dark: deepPurple[700],
    },
    secondary: {
      main: '#529E3C',
    },
  },
});